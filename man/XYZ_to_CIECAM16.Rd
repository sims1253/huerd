% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ciecam16.R
\name{XYZ_to_CIECAM16}
\alias{XYZ_to_CIECAM16}
\title{Convert CIE XYZ to CIECAM16 Color Appearance Correlates}
\usage{
XYZ_to_CIECAM16(
  XYZ,
  XYZ_w,
  L_A,
  Y_b,
  surround = ciecam_environs("Average"),
  discount_illuminant = FALSE,
  compute_H = TRUE
)
}
\arguments{
\item{XYZ}{Numeric vector of length 3 or matrix (Nx3) containing CIE XYZ
tristimulus values. Values should be in the domain \link{0, 100}.}

\item{XYZ_w}{Numeric vector of length 3 containing the reference white point
XYZ values. Values should be in the domain \link{0, 100}.}

\item{L_A}{Numeric. The adapting field luminance in cd/m². Must be positive.}

\item{Y_b}{Numeric. The relative luminance of background (Y_b = 100 * L_b / L_w).
Must be positive.}

\item{surround}{List of viewing condition parameters from \code{ciecam_environs()}.
Default is "Average".}

\item{discount_illuminant}{Logical. If TRUE, assumes complete chromatic
adaptation. Default is FALSE.}

\item{compute_H}{Logical. If TRUE, computes hue quadrature (H). Default is TRUE.}
}
\value{
A list containing CIECAM16 color appearance correlates:
\describe{
\item{J}{Lightness (0-100)}
\item{C}{Chroma (≥0)}
\item{h}{Hue angle in degrees (0-360)}
\item{s}{Saturation (0-100)}
\item{Q}{Brightness (≥0)}
\item{M}{Colorfulness (≥0)}
\item{H}{Hue quadrature (0-400) if compute_H=TRUE}
\item{HC}{Hue composition (NA in current implementation)}
}
}
\description{
Transforms CIE XYZ tristimulus values to CIECAM16 color appearance correlates
using the forward transform of the CIECAM16 color appearance model.
}
\examples{
# Convert D65 white point
XYZ <- c(95.05, 100.00, 108.88)
XYZ_w <- XYZ # Using same white point
L_A <- 318.31 # Typical office lighting
Y_b <- 20.0 # Typical background
result <- XYZ_to_CIECAM16(XYZ, XYZ_w, L_A, Y_b)

# Convert with dim viewing conditions
dim_result <- XYZ_to_CIECAM16(XYZ, XYZ_w, L_A, Y_b,
  surround = ciecam_environs("Dim")
)

}
