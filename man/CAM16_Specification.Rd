% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ciecam16.R
\name{CAM16_Specification}
\alias{CAM16_Specification}
\title{CAM16 Color Specification Class}
\usage{
CAM16_Specification(
  J = numeric(),
  C = numeric(),
  h = numeric(),
  s = numeric(),
  Q = numeric(),
  M = numeric(),
  H = numeric(),
  e = numeric()
)
}
\arguments{
\item{J}{A numeric vector representing lightness (0-100), or a single numeric value.}

\item{C}{A numeric vector representing chroma (0-100), or a single numeric value.}

\item{h}{A numeric vector representing hue angle in degrees (0-360), or a single numeric value.}

\item{s}{A numeric vector representing saturation (0-100), or a single numeric value.}

\item{Q}{A numeric vector representing brightness (0-100), or a single numeric value.}

\item{M}{A numeric vector representing colorfulness (0-100), or a single numeric value.}

\item{H}{A numeric vector representing hue quadrature (0-400), or a single numeric value.}

\item{HC}{A character vector representing hue composition (e.g., "35R65Y"), or a single character value.}
}
\value{
Either a single \code{CAM16_Specification} object (if all inputs are of length 0 or 1), or a list of \code{CAM16_Specification} objects if any input has length greater than 1.
}
\description{
A class for representing colors in the CIECAM16 color appearance model space.
This model provides a comprehensive way to describe colors in terms of their
perceptual attributes like lightness, chroma, and hue.

The constructor \code{CAM16_Specification()} can accept either single values or vectors
for its properties (J, C, h, s, Q, M, H, HC). If all arguments are either empty or length 1,
it returns a single \code{CAM16_Specification} object. If any argument has a length greater
than 1, the constructor returns a list of \code{CAM16_Specification} objects, where each
element in the list corresponds to a set of input values.
}
\details{
The CIECAM16 color specification includes the following properties:
\itemize{
\item \code{J}: Lightness (0-100)
\item \code{C}: Chroma (0-100)
\item \code{h}: Hue angle in degrees (0-360)
\item \code{s}: Saturation (0-100)
\item \code{Q}: Brightness (0-100)
\item \code{M}: Colorfulness (0-100)
\item \code{H}: Hue quadrature (0-400)
\item \code{HC}: Hue composition (e.g., "35R65Y")
}
}
\section{Slots}{

\describe{
\item{\code{J}}{A numeric value representing lightness (0-100).}

\item{\code{C}}{A numeric value representing chroma (0-100).}

\item{\code{h}}{A numeric value representing hue angle in degrees (0-360).}

\item{\code{s}}{A numeric value representing saturation (0-100).}

\item{\code{Q}}{A numeric value representing brightness (0-100).}

\item{\code{M}}{A numeric value representing colorfulness (0-100).}

\item{\code{H}}{A numeric value representing hue quadrature (0-400).}

\item{\code{HC}}{A character string representing hue composition (e.g., "35R65Y").}
}}

\examples{
# Create a single CAM16_Specification object
single_color <- CAM16_Specification(J = 50, C = 30, h = 180)

# Create a list of CAM16_Specification objects
multiple_colors <- CAM16_Specification(J = c(50, 60), C = c(30, 40), h = c(180, 200))

# Create an empty object
empty_color <- CAM16_Specification()
}
\references{
CIE (2022). CIE 248:2022 The CIE 2016 Colour Appearance Model for Colour
Management Systems: CIECAM16.
}
