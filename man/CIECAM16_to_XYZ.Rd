% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ciecam16.R
\name{CIECAM16_to_XYZ}
\alias{CIECAM16_to_XYZ}
\title{Convert CIECAM16 Color Appearance Correlates to CIE XYZ}
\usage{
CIECAM16_to_XYZ(
  CAM,
  XYZ_w,
  L_A,
  Y_b,
  surround = ciecam_environs("Average"),
  discount_illuminant = FALSE
)
}
\arguments{
\item{CAM}{List containing CIECAM16 correlates with required elements:
\describe{
\item{J}{Lightness (0-100)}
\item{C}{Chroma (≥0)}
\item{h}{Hue angle in degrees (0-360)}
}}

\item{XYZ_w}{Numeric vector of length 3 containing the reference white point
XYZ values. Values should be in the domain \link{0, 100}.}

\item{L_A}{Numeric. The adapting field luminance in cd/m². Must be positive.}

\item{Y_b}{Numeric. The relative luminance of background (Y_b = 100 * L_b / L_w).
Must be positive.}

\item{surround}{List of viewing condition parameters from \code{ciecam_environs()}.
Default is "Average".}

\item{discount_illuminant}{Logical. If TRUE, assumes complete chromatic
adaptation. Default is FALSE.}
}
\value{
Numeric vector of length 3 containing CIE XYZ tristimulus values
in the domain \link{0, 100}.
}
\description{
Transforms CIECAM16 color appearance correlates back to CIE XYZ tristimulus
values using the inverse transform of the CIECAM16 color appearance model.
}
\examples{
# Convert a medium gray in CIECAM16 space back to XYZ
CAM <- list(J = 50, C = 0, h = 0) # Achromatic color
XYZ_w <- c(95.05, 100.00, 108.88) # D65 white point
L_A <- 318.31
Y_b <- 20.0
XYZ <- CIECAM16_to_XYZ(CAM, XYZ_w, L_A, Y_b)

# Convert a saturated red
red_CAM <- list(J = 50, C = 50, h = 0)
red_XYZ <- CIECAM16_to_XYZ(red_CAM, XYZ_w, L_A, Y_b)

}
