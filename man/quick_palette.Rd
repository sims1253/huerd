% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience.R
\name{quick_palette}
\alias{quick_palette}
\title{Quick palette generation with sensible defaults}
\usage{
quick_palette(
  n,
  brand_colors = NULL,
  cvd_safe = TRUE,
  quality = c("balanced", "fast", "high"),
  lightness = "any"
)
}
\arguments{
\item{n}{Number of colors to generate.}

\item{brand_colors}{Optional character vector of hex colors that must be
included in the palette. These colors will be preserved exactly as
provided, and additional colors will be optimized around them.}

\item{cvd_safe}{Logical. If \code{TRUE} (default), the optimizer maximizes
the worst-case perceptual distance across color vision deficiency
simulations (deuteranopia, protanopia, tritanopia). If \code{FALSE}, it
optimizes for normal vision only.}

\item{quality}{Character string specifying the quality/speed tradeoff:
\itemize{
\item \code{"fast"}: Quick generation with fewer iterations (good for exploration)
\item \code{"balanced"}: Default balance of quality and speed
\item \code{"high"}: More iterations for better results (slower)
}}

\item{lightness}{Character string or numeric vector specifying lightness
constraints:
\itemize{
\item \code{"any"}: Balanced range (L: 0.2-0.9)
\item \code{"light"}: Prefer lighter colors (L: 0.5-0.9)
\item \code{"dark"}: Prefer darker colors (L: 0.2-0.6)
\item \code{"mid"}: Prefer mid-range lightness (L: 0.35-0.75)
\item Numeric vector of length 2: Custom bounds (e.g., \code{c(0.3, 0.8)})
}}
}
\value{
A \code{huerd_palette} object (character vector of hex colors with
additional attributes).
}
\description{
A simplified interface to \code{\link[=generate_palette]{generate_palette()}} that uses intuitive parameter
names and sensible defaults. This function is designed for users who want
good results without understanding optimization details.
}
\examples{
# Simple 5-color palette
quick_palette(5)

# Include brand colors
quick_palette(6, brand_colors = c("#1f77b4", "#ff7f0e"))

# Fast generation for exploration
quick_palette(8, quality = "fast")

# Light colors for dark backgrounds
quick_palette(5, lightness = "light")

}
\seealso{
\code{\link[=generate_palette]{generate_palette()}} for full control over palette generation.
}
