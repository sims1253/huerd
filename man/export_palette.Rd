% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience.R
\name{export_palette}
\alias{export_palette}
\title{Export palette to various formats}
\usage{
export_palette(
  palette,
  format = c("hex", "css", "sass", "json", "csv"),
  names = NULL,
  file = NULL
)
}
\arguments{
\item{palette}{A \code{huerd_palette} object or character vector of hex colors.}

\item{format}{Output format. One of:
\itemize{
\item \code{"hex"}: Character vector of hex colors (default)
\item \code{"css"}: CSS custom properties (variables)
\item \code{"sass"}: Sass/SCSS variables
\item \code{"json"}: JSON object keyed by color names
\item \code{"csv"}: CSV format with color names
}}

\item{names}{Optional character vector of names for the colors. If \code{NULL},
colors are named \code{color_1}, \code{color_2}, etc.}

\item{file}{Optional file path to write the output. If \code{NULL}, returns the
formatted string. If specified, the function writes the formatted palette
to the file and (invisibly) returns the file path.}
}
\value{
If \code{file} is \code{NULL}: for \code{"hex"}, the palette as a character
vector (returned invisibly); for all other formats, the formatted
palette as a single character string. If \code{file} is specified, the
function writes the formatted palette to the file and (invisibly)
returns the file path.
}
\description{
Export a huerd palette to common design and development formats.
}
\examples{
pal <- generate_palette(5, progress = FALSE)

# Get as hex vector (default)
export_palette(pal)

# CSS custom properties
export_palette(pal, format = "css")

# With custom names
export_palette(pal, format = "css",
               names = c("primary", "secondary", "accent", "bg", "text"))

# JSON for web applications
export_palette(pal, format = "json")

}
